# Art Folders

Place the current featured PNG in `art/weekly`.

Place previous pieces in `art/gallery`.

GitHub Pages automatically rebuilds `art-manifest.json` from these folders when
you publish the repository. If there is more than one image in `art/weekly`, the
site features the image whose filename sorts newest first. Date-style filenames
work well, for example `2026-05-19-butterfly.png`.
